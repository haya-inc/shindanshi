---
title: ようこそ
description: 初期化直後の確認ポイント
---

## このプロジェクトについて

このアプリは、`pnpm`、`Next.js 16`、`Fumadocs`、`Vercel` を前提に初期化されています。

## 最初に確認すること

1. `pnpm dev` でアプリが起動すること
2. `/docs` で Fumadocs のページが表示されること
3. `pnpm lint` と `pnpm build` が通ること

## 補足

リポジトリ内の設計判断や運用指針は `docs/` 配下にも整理しています。
